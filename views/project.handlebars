{{#extends 'main'}}

{{#content 'body'}}
  <h1>{{project.name}}</h1>
  <p>{{project.description}}</p>
  <p>Needed Funding: ${{project.needed_funding}}</p>
  <p>Date Created: {{project.date_created}}</p>

  <a href="/">Back to Home</a>
{{/content}}
